import React from 'react';
import './Header.scss';

export const Header = () => {
    return(
        <header id="header">
            <p className="text-center">
                <span className='logo'>M</span>oments
            </p>
        </header>
    );
};